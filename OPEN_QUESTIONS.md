# Breeze — Open Questions

These are phase-owned questions. Deferred questions are intentionally not current blockers.

1. **Resolved in PROMPT 9:** Application information architecture, authenticated workspace boundary, routing surfaces, and responsive visual system are finalized in `BREEZE_SPEC.md`.
2. **RESOLVED — OWNED BY COMPLETED PROMPT 2:** Exact PostgreSQL types, indexes, constraints, nullability, cascade behavior, and migration tooling for the frozen conceptual tables.
3. **Resolved in PROMPT 3:** Identity hierarchy, precedence, conflict behavior, confidence/method/evidence semantics, and audit behavior are finalized in `BREEZE_SPEC.md`.
4. **Resolved in PROMPT 4:** Journey, issue-grouping, issue-lifecycle, and escalation algorithms are finalized in `BREEZE_SPEC.md`.
5. **Resolved in PROMPT 5:** Churn outcomes, cohorts, feature extraction, analytics formulas, sufficiency statuses, and association boundaries are finalized in `BREEZE_SPEC.md`.
6. **Resolved in PROMPT 8:** Default reports, formulas, report configuration/validation, runs, snapshots, artifacts, retention, and report semantics are finalized in `BREEZE_SPEC.md`.
7. **Resolved in PROMPT 6:** Registration/onboarding behavior, capability-specific configuration/readiness, and the `Breeze Demo Retail` controlled configuration are finalized in `BREEZE_SPEC.md`.
8. **Resolved in PROMPT 7:** Exact Gemini model/configuration and scoped MCP tool contracts are finalized in `BREEZE_SPEC.md`.
9. **Resolved in PROMPT 9:** Final user-facing information architecture and responsive visual system are finalized in `BREEZE_SPEC.md`.
10. **Resolved in PROMPT 6:** The conceptual registration/mock-company boundary is finalized; implementation remains later scope.

## Unresolved Prompt 9 questions

None. Canonical Prompt 10 implementation remains incomplete, but implementation incompleteness is not a Prompt 9 specification question.

## Prompt 9.6 resolution

The four Prompt 9.5 readiness questions are resolved: Supabase Auth provides persistent identity/session state without changing the 21 domain tables; company verification is self-attested with non-unique display names and safe duplicate-email handling; custom reports are company-scoped with immutable ReportRun snapshots and independent artifact expiry; and MCP remains exactly the 13 Prompt 7 read-only tools with report operations outside MCP.

## Unresolved Prompt 9.6 questions

None. Prompt 10 may select compatible library versions and deployment details; those are implementation choices, not unresolved specification decisions.
